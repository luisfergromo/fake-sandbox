name: A workflow for my Hello World
on: [push]
jobs:
  build:
    name: Hello World action
    runs-on: ubuntu-latest
    steps:
      - name: Saying Hello from workflows 
        run: |
          echo "$GITHUB_ACTOR, how u doing?"
